# Neinote MVP 核心工作流程图
::: mermaid
graph TD;
    A[用户注册/登录] --> B{灵感捕获};
    B -->|文字| B1[快速文本卡片输入];
    B -->|语音| B2[语音转文本输入-未来];
    B -->|网页| B3[网页剪藏 未来];
    B1 --> C{卡片管理};
    B2 --> C{卡片管理};
    B3 --> C{卡片管理};

    C --> C1[手动组织与连接卡片];
    C --> C2[AI 辅助卡片连接 未来];
    C1 --> D{文章生成};
    C2 --> D{文章生成};

    D -->|可AI生成| D1[选择相关卡片];
    D1 --> |AI整合| D2[生成文章初稿];
    D2 --> |文本编辑器| E[文章润色修改];
    E[文章润色修改] --> |选择平台| F[发布/导出文章];
    F -->|作为参考| G[存档迭代];

:::

## 流程说明：

此流程图描绘了 Neinote 产品的核心 MVP 功能，以及未来可能扩展的功能。

-   **用户注册/登录**：用户进入系统的第一步。
-   **灵感捕获**：用户通过快速文本卡片输入来记录想法。语音转文本和网页剪藏是未来计划的功能。
-   **卡片管理**：用户可以手动组织和连接卡片，构建知识网络。AI 辅助连接是未来计划的功能。
-   **文章生成**：用户选择相关卡片后，AI 会根据这些卡片生成文章初稿。
-   **文章编辑**：用户使用基础文本编辑器对生成的文章进行修改和完善。
-   **发布/导出文章**：完成编辑后，用户可以将文章发布或导出。
-   **用户反馈与迭代**：产品将持续收集用户反馈并进行迭代优化。

**MVP 核心功能**：图中用子图框起来的部分是当前 MVP 阶段计划实现的核心功能。
**虚线箭头**：表示未来可能实现的功能或可选路径。